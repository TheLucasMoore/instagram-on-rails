
<div class="text-align-center">
<%= image_tag @post.image.url(:medium) %>
<h4><%= link_to "Delete this post", post_path(@post), method: :delete, data: {confirm: "Are you sure you want to delete this forever?"} %></h4>
<%= render 'form' %>
</div>